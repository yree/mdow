fuse:
  dir: "/litefs"

data:
  dir: "/var/lib/litefs"

proxy:
  addr: ":8080"
  target: "localhost:8081"
  db: "mdow.db"

exec:
  - cmd: "/usr/local/bin/mdow"
    if-primary: true

lease:
  type: "consul"
  consul:
    url: "${FLY_CONSUL_URL}"
    key: "litefs/${FLY_APP_NAME}" 